import React from "react";

export default class Button extends React.Component {
    render(): React.ReactNode {
        return (
            <button>
                Botão
            </button>
        )
    }
}